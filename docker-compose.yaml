services:
  swarmauri-com:
    build: .
    container_name: swarmauri-com
    restart: unless-stopped
    labels:
      site.host: swarmauri.com
      site.kind: mdwrk-lander
    ports:
      - "127.0.0.1:0:80"
